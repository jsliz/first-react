{
  "name": "first-app",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "https://github.com/jsliz/first-react.git",
  "author": "Jarek <jarek@builderdesigns.com>",
  "license": "MIT",
  "dependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.0",
    "babel-preset-react": "^6.24.1",
    "serve": "^6.1.0"
  },
  "scripts": {
    "build": "babel src/app.js --out-file=public/scripts/app.js --presets=env,react",
    "build-watch": "babel src/app.js --out-file=public/scripts/app.js --presets=env,react --watch",
    "serve": "serve -p 3000 public/"
  }
}
